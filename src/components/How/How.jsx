import React from "react";
import './How.css';
function How(){
    return(
      <div className="how" id="how">
        <div>
        <h1>HOW WE BUILD</h1>
        <h2>WITH HYDRA VR?</h2>
        </div>
        <p>The id selector uses the id attribute of an 
        HTML element to select a specific element. The id of an element is unique within a page, so the id selector is used to select one unique element! To select an element with a specific id</p>
      </div>
    );
}
export default How;